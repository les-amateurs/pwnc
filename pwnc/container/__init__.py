"""
docker and podman
create named persistent container instances
atomic way of keeping track of used and unused container
solution for now is to make everything ephemeral
each gets dedicated host share folders, with most files ro mounted into the container

look at Dog
"""

